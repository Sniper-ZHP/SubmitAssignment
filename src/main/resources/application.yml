spring:
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tyut?serverTimezone=Asia/Shanghai
    username: 
    password: 

mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml
  config-location: classpath:mybatis/mybatis-config.xml
  type-aliases-package: edu.tyut.assignsub.pojo

aliyun:
  endpoint: 
  accessKeyId: 
  accessKeySecret: 
  bucketName: 
  urlPrefix: 